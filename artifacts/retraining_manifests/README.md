# Retraining Manifests

Generated retraining manifest files can be written here by the Phase 8 export flow.

Only doctor/admin confirmed or corrected labels are eligible. Pending reviews and raw AI predictions must not be exported as training labels.

Do not commit generated manifest files.
