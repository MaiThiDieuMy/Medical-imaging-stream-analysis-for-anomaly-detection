# Local Model Weights

Place local model checkpoints here when developing or testing local inference.

Expected examples:

- `artifacts/models/best_model.pth` for the Kaggle MobileNetV3-Small checkpoint.
- `artifacts/models/chest_xray_densenet121.pt` for older local experiments.

These files are intentionally ignored by Git. Do not commit model weights.
