# Evaluation Manifests

Place fixed holdout evaluation manifests here. These files are ignored by Git.

Retraining uses `RETRAIN_EVAL_MANIFEST_PATH` when the configured file exists. The
holdout images must not be reused in replay/train data.
