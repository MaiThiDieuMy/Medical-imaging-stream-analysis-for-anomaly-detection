# Local Datasets

Place local training/replay datasets here. These files are ignored by Git.

The retraining replay CSV should contain:

```csv
image_path,label,source
/app/artifacts/datasets/chest-xray-dataset_for_finetune/Atelectasis/example.png,Atelectasis,sampled_from_initial_training
```
